# HealthEye

Agentic elderly home monitoring using Amazon Bedrock + deterministic anomaly checks. Demo via Streamlit shows KB retrieval + anomaly detection + snooze/core memory + escalation/acknowledgement.

Quickstart:
1) pip install -r requirements_minimal.txt
2) streamlit run streamlit_app.py